# UI shell knobs (optional — names only)

# Maintenance Depot keeps brand via CSS tokens in resources/css/app.css
# (--color-brand-*). Do not inject PilotBPM purple --primary.

# Optional future: runtime tenant brand override (project-owned):
# VITE_BRAND_PRIMARY=#e7660b
