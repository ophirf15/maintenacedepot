# Search knobs (optional — names only)

# GET /api/search debounce is client-side (~180ms).
# SEARCH_RESULT_LIMIT default 12 (max 25) via ?limit=
# AI Ask row intentionally disabled for Maintenance Depot.
